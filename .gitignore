distfiles/
new_root/
Makefile
live/
stage*.tar
rootfs.tar.xz
repository/Manifest.yml

# Files created in these directories are a) large, b) not unique to digitalis, and
# c) possibly incompatible with the license terms of the general repository. They
# therefore should not be uploaded to GitHub.
s2repo/builds
s3repo/builds
repository/builds
repository/sources

