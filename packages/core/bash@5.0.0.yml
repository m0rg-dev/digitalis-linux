version: "5.0"
rdepend:
  - libs/ncurses
filename: bash
comp: tar.gz
upstream: https://ftp.gnu.org/gnu/bash/
additional_configure_options: --bindir=/bin
post_install_script: ln -s bash bin/sh
queue_hooks:
  ldconfig: true