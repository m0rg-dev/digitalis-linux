version: "2.0.5"
bdepend:
  - util/cmake
  - lang/nasm
filename: libjpeg-turbo
comp: tar.gz
upstream: https://downloads.sourceforge.net/libjpeg-turbo/
license: IGJ AND BSD-3-Clause AND zlib
use_build_dir: true
configure: |
  cmake -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=release -DCMAKE_INSTALL_DEFAULT_LIBDIR=/lib ../%{unpack_dir}
