version: "1.1.3"
upstream: ~
src: ~
license: MIT
use_build_dir: true
configure: ~
make: ~
install: |
  python3 -m ensurepip
  python3 -m pip install --no-use-pep517 --root=/target_root mako==%{version}
