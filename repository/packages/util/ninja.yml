version: "1.10.0"
filename: ninja
src_url: https://github.com/ninja-build/ninja/archive/v%{version}.tar.gz
comp: tar.gz
license: Apache-2.0
configure: ./configure.py --bootstrap
make: ~
install: |
  mkdir -p /target_root/usr/bin
  cp ninja /target_root/usr/bin
